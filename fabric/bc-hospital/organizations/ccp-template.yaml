---
name: test-network-${ORG}
version: 1.0.0
client:
  organization: ${ORGMSP}
  connection:
    timeout:
      peer:
        endorser: '300'
organizations:
  ${ORGMSP}:
    mspid: ${ORGMSP}MSP
    peers:
    - peer0.${ORG}.arsada.org
    certificateAuthorities:
    - ca.${ORG}.arsada.org
peers:
  peer0.${ORG}.arsada.org:
    url: grpcs://localhost:${P0PORT}
    tlsCACerts:
      pem: |
          ${PEERPEM}
    grpcOptions:
      ssl-target-name-override: peer0.${ORG}.arsada.org
      hostnameOverride: peer0.${ORG}.arsada.org
certificateAuthorities:
  ca.${ORG}.arsada.org:
    url: https://localhost:${CAPORT}
    caName: ca-${ORG}
    tlsCACerts:
      pem: 
        - |
          ${CAPEM}
    httpOptions:
      verify: false
